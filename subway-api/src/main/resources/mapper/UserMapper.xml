<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metrohub.api.domain.user.UserMapper">

    <resultMap id="UserResultMap" type="com.metrohub.api.domain.user.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="role" column="role"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password, nickname, role, created_at)
        VALUES (#{email}, #{password}, #{nickname}, COALESCE(#{role}, 'USER'), NOW())
    </insert>

    <select id="findByNickname" resultMap="UserResultMap">
        SELECT * FROM users WHERE nickname = #{nickname}
    </select>

    <select id="findById" resultMap="UserResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="existsByNickname" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE nickname = #{nickname}
    </select>

</mapper>
