apiVersion: v1
kind: Service
metadata:
  name: subway-api
  namespace: metrohub
spec:
  selector:
    app: subway-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
