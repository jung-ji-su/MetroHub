<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.metrohub.notification.mapper.NotificationMapper">

    <resultMap id="notificationResultMap" type="com.metrohub.notification.domain.Notification">
        <id property="id" column="id"/>
        <result property="type" column="type"/>
        <result property="title" column="title"/>
        <result property="body" column="body"/>
        <result property="referenceId" column="reference_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (type, title, body, reference_id)
        VALUES (#{type}, #{title}, #{body}, #{referenceId})
    </insert>

    <select id="findAll" resultMap="notificationResultMap">
        SELECT id, type, title, body, reference_id, is_read, created_at
        FROM notifications
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="findByType" resultMap="notificationResultMap">
        SELECT id, type, title, body, reference_id, is_read, created_at
        FROM notifications
        WHERE type = #{type}
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM notifications
    </select>

    <select id="countByType" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE type = #{type}
    </select>

</mapper>
